Jira: ENR-207
Status: ready-for-agent — Jira status on 2026-08-22 is `QA`; not transitioned. The panel and the imagery were settled in grilling 2026-08-22; B4.1's story edit stays a human Jira write (`../review-2026-08-21/ledger.md` §5.3–5.4)
Labels: design, persona-student, screen-housing, wave-mvp-demo
Jam: (none)
Sources: `../review-2026-08-21/sources/housing-changes-audentra.md` (G1–G11; copy §6; order §8); `../review-2026-08-21/sources/decisions-and-conformance.md` B4.1–B4.5, §12; `../review-2026-08-21/sources/my-financials-changes-audentra.md` 9.4; `../review-2026-08-21/ledger.md`

# Housing — changes from the review of 2026-08-21

## Agent Brief

**Category:** enhancement (B4.2–B4.4 are conformance gaps against ENR-210/ENR-211, Prioritized)
**Summary:** The student chooses where she will live from a picture, not a monogram; three states stop
rendering as the same page; the page says in every state whether it is asking her for something; the
shortlist is reachable from the catalogue; the rail stops repeating the main column; the catalogue can
be filtered; and the four-answer plan stands, with the story updated to match.

**Current behavior** (confirmed 2026-08-22): residence cards lead with a two-letter monogram
(`.org-tile` initials). The plan offers four answers ("Living on campus", "Commuting", "Arranging my
own housing", "I need help deciding"). `ready`, `empty` and `partial` render identically until the
student picks an answer. Only `onboarding-answered` states that the answer is recorded. There is no
summary panel — **removed on purpose after the Jam of 2026-08-21** (`5e68b80`; the page's comment
says the plan card is the figure and the band the deadline) — and no advisor block, no band. The
catalogue's "Your shortlist is full. Remove one to add this" offers no way to the shortlist. The rail
holds the deadline anchor card (with Housing Services' location, hours and reply time) and one
"What a preference is worth" card that quotes the "Changing your housing answer" guide in full. The
catalogue sorts ("Lowest rate", "Closest") and does not filter. "I need help deciding" routes to the
Help page ("Ask Housing Services"). Residence names render at 15px in two weights; prices and
monograms at 13.5px; "A preference is not an assignment." at 13.5px bold.

**Desired behavior**, in the document's execution order:

1. **G2 — the answer set.** The four answers stay; they are the correct set and the onboarding spec and
   ENR-210 adopt them (B4.1 — Jira and an external document; not this card). Where an onboarding
   answer of "Off campus" is held, the portal does not guess: it shows the answer as recorded and asks
   the one question that separates commuting from arranging her own housing, once (copy §6.2).
2. **G3, G4 — the states tell the truth before she acts.** The second-question block states the
   catalogue's condition in `empty` and `partial` before the first question is answered (copy §6.3).
   In **every** state the page states whether it is asking her for something: recorded at onboarding
   (nothing asked; changeable until the deadline) · skipped at onboarding (on her checklist, due date)
   · answered here (recorded, changeable) · after the deadline (submitted, Housing Services assigning)
   · room assigned (the room, plan closed). Copy §6.1. It lives in the summary panel (3).
3. **G5, G6 — the summary panel returns** (decided 2026-08-22; the Jam of 2026-08-21 removed a panel
   whose figure was the plan itself; this one's figure is the plan's *standing*, which moves, and the
   workflow's test is met the way Health met it — `docs/agents/design-workflow.md` records it).
   `SummaryFigure`: the plan status as the figure, the deadline with days remaining or the assignment
   as the support line, "n of 3 residences ranked" as the second line when the plan is on campus;
   `AdvisorBar` with eyebrow "YOUR ENROLLMENT ADVISOR" and the scope line "For anything about
   enrollment. Your housing plan and your room are Housing Services' to decide." (the slot for the
   scope line follows the Health precedent). No housing action routes to him. The `notice` slot keeps
   the failure footnote it has today.
4. **G1 enlarged by B4.3 — the residence has a picture, a detail and a comparison.** The card's leading
   slot carries a published image, fixed ratio, cropped to fill; the monogram survives only as the
   fallback, and the fallback is never stock photography and never generated; an image shows the
   residence, never a specific room; where a bedroom is shown the caption names the room type. The
   card opens a **residence detail** carrying the image at full size, the room types, the building,
   the rates and what each rate includes (ENR-211 AC2); a **compare** surface is the second view and
   is where the filters (6) live. **The assets** (decided 2026-08-22, now a CLAUDE.md rule): stock
   building photography from Unsplash — exteriors and shared spaces of residence halls, one per
   residence hall in the data (eight), chosen by hand to read as a US campus — under
   `public/residences/`, with a `SOURCES.md` written the way `public/people/SOURCES.md` is (licence,
   photographer where known, fetch parameters, what a picture may and may not show). Never a
   photograph of students, never a staged bedroom without a caption naming the room type, never
   generated; the monogram is the fallback for a hall with no picture on file.
5. **B4.4 — a partial shortlist is a state.** One or two ranked reads as partial ("1 of 3 ranked"),
   saves as partial, and the housing item stays outstanding until three are ranked or the deadline
   passes (ENR-211 AC9).
6. **G10 — filters.** Room type, meal plan and a price ceiling, on the attributes the cards show; a
   combination that would return nothing is disabled and states why ("No residences match this with
   your other filters."); the two sorts stay. On the compare surface (4).
7. **G8 — the shortlist is one interaction away.** "Your shortlist is full. See your shortlist to swap
   one." is an action that takes her to the shortlist, and the shortlist stays reachable while she
   scrolls the catalogue.
8. **G9 — the rail.** Keep the deadline anchor card and Housing Services' facts. The explanation card
   keeps its title and its body reduces to "You tell Housing Services what you'd like, in order. They
   decide, and they may place you somewhere you didn't name." plus a link "How housing decisions
   work" that opens the fuller explanation and the "Changing your housing answer" guide (the
   "How points work" shape, `InfoModal`). No explanation appears in full in both columns.
9. **B4.2 — "I need help deciding" routes to a person, through Edward.** Choosing it opens the route
   to Housing Services by the escalation the ENR-181 card builds (no direct line before Edward, Part A
   §12), the plan stays open, and the housing item stays outstanding with its deadline. Until the door
   exists, the current `Ask Housing Services` stays rather than routing nowhere.
10. **G7 — the band**, last, because it points at the plan and at the shortlist: a rejected change →
    "Housing Services didn't accept that change" / "Try again"; first question unanswered → "Your
    housing plan is due Dec 15" / "Choose your plan" (focuses the first question); plan on campus and
    shortlist not full → "You've ranked 1 of 3 residences" / "Rank residences" (scrolls to the
    catalogue); otherwise no band. It never asks for something the deadline has closed and never
    renders after a room is assigned.
11. **G11, 9.4 — hierarchy.** Nothing collapses. Block and residence titles `--fs-h2`/`--fw-regular`,
    never bolder than the section above; the monogram fallback at the name's step; prices
    `--fs-body` (a catalogue row is not a summary — no lead figure on this screen); "A preference is
    not an assignment." at `--fs-h2`/`--fw-regular`. 13.5px and 15px appear nowhere in the content
    column.

**Key interfaces:** the residence card gains an image slot with the initials fallback; a residence
detail (`Drawer`) and a compare surface; the shortlist state gains *partial*; the plan status is
readable by `PageShell`'s docked sentence or panel; the catalogue filters read the attributes the cards
already carry; the rail explanation moves behind `InfoModal`; the band is `ActionBand`.

**Acceptance criteria:**

- [ ] The two surfaces (portal, onboarding) offer the same four answers and no answer is silently
      converted; an "Off campus" onboarding answer is reconciled by one question, once (G2).
- [ ] `ready`, `empty` and `partial` are distinguishable without picking an answer (G3).
- [ ] In every state the page states whether it is asking for something, with the deadline or the
      assignment (G4).
- [ ] Every residence card shows a published image or the monogram fallback; no image implies a room
      type the card does not name; the card opens a detail with room, building and rates; residences
      can be compared (G1, B4.3).
- [ ] A shortlist of one or two reads as partial, saves, and keeps the item outstanding (B4.4).
- [ ] Every attribute shown on a card can be filtered on; no filter produces an empty list (G10).
- [ ] From any card reporting a full shortlist, the shortlist is one interaction away (G8).
- [ ] No explanation appears in full in both the main column and the rail (G9).
- [ ] "I need help deciding" opens a route to Housing Services, the plan stays open and the item stays
      outstanding (B4.2).
- [ ] The band never asks for something the deadline has closed and never renders after a room is
      assigned (G7).
- [ ] No title is bolder than `--fw-regular` or smaller than its parent; 13.5px and 15px are absent
      from the content column; no figure leads (G11, 9.4).
- [ ] `npm run build` passes; 1440 and 390 checked; the seven inspected states re-inspected.

**Out of scope:** ENR-210/onboarding-spec edits (human); the Financials housing figure (ENR-166,
B4.5); the My Degree rail change the housing document carries in its §9.1 (ENR-188); building the
Edward door (ENR-181).

**What is right and must survive** (§3): "A preference is not an assignment" and the closed loop in
`room-assigned`; all four answers framed as real; each answer's consequence; the assignment names
Nadia Aslam; "Changing your housing answer" (now behind the link); `send-fails` names the surviving
record.

**Domain:** Housing Services owns the plan, the ranking and the assignment; the enrollment advisor is
presence, not routing (rule 3). The deadline is Dec 15 and the move-in Jan 12 in Aster's calendar —
read `docs/domain/aster.md` (being written 2026-08-22) before writing a date, and
`docs/domain/us-enrollment.md` for what a US housing office does and calls things ("residence hall",
"meal plan", "room assignment").

**Blocked by:** `../review-2026-08-21/brief.md` (foundations); ENR-181 for item 9.
